# Copy to .env for local development. Never commit .env (gitignored).
APP_ENVIRONMENT=local
APP_LOG_LEVEL=INFO
# "memory" for local dev/tests (no AWS needed), "dynamodb" in the cluster
APP_REPOSITORY_BACKEND=memory
APP_AWS_REGION=us-east-1
APP_DYNAMODB_TABLE=restaurants
